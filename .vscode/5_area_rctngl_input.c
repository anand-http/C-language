#include <stdio.h>
int main()
{

    // CALCULATE AREA OF RECTANGLE BY TAKING INPUT FROM USER

    int l, b;
    printf("Enter the length and breadth of rectangle\n");
    scanf("%d%d", &l, &b);

    printf("the area of rectangle is %d", l * b);
    return 0;
}