#include <stdio.h>
#include <string.h>
int main()
{
    // C program to get the length of string by  using a function strlen

    char *ptr = "anand";
    int a = strlen(ptr);
    printf("the length of string char is %d", a);
    return 0;
}